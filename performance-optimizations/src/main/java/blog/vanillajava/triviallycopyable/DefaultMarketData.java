package blog.vanillajava.triviallycopyable;

/**
 * A simple, default implementation of {@link MarketData} that inherits its behavior
 * without adding any custom serialization or deserialization logic.
 *
 * <p>This class is useful as a baseline or placeholder implementation when the default
 * behavior provided by {@link MarketData} is sufficient for the application's needs.</p>
 *
 * <p>Since this class does not override any methods, it relies entirely on the
 * functionality of its superclass, including any default serialization mechanisms.</p>
 */
public final class DefaultMarketData extends MarketData {
    // No additional fields or methods are defined in this class.
    // This class serves as a direct, unmodified extension of MarketData.
}
